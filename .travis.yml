language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-vala' ]; then cd .. && eval "mv $currentfolder generator-vala" && cd generator-vala; fi

