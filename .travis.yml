language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-elementary' ]; then cd .. && eval "mv $currentfolder generator-elementary" && cd generator-elementary; fi
